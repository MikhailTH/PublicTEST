name: CI

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: blairhudson/jupydiff@master
      - uses: actions/core
#         with:
#           github-token: ${{secrets.GITHUB_TOKEN}}
