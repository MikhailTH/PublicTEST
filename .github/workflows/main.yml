name: CI

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - id: foo
        uses: blairhudson/jupydiff@v0.0.1
      - name: script
        uses: actions/github-script@v1
        
      - uses: actions/cache@v2
