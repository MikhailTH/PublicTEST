name: CI

on:
  push:

jobs:
  marinates:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 1
        
      - uses: blairhudson/jupydiff@master
